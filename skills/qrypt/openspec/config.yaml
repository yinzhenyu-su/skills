schema: spec-driven

# Qrypt - 夸克网盘 rclone 兼容加密挂载工具
# 项目上下文：AI 创建制品时显示此信息
context: |
  Tech stack: Go, cgofuse (FUSE), cobra (CLI)
  Tech stack: rclone crypt (scrypt / EME-AES / NaCl Secretbox)
  Tech stack: SQLite (chunk cache), TOML config
  Platform: macOS (macFUSE 4.0+) | Linux (libfuse)
  Conventions: Chinese CLI output, rust_decimal-style typed sizes
  Domain: cloud drive FUSE mount with encryption
