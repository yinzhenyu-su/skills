## 1. 删除一致性收敛

- [x] 1.1 梳理并统一 Unlink/Rmdir 成功后的本地清理入口（node、pending、dirty、mem cache）
- [x] 1.2 增加按 path/fid 双维度清理逻辑，避免删除后残留重试任务
- [x] 1.3 失败删除路径保持可重试状态并返回明确错误

## 2. 修改写回稳定化

- [x] 2.1 统一 Write/Flush（及等价完成点）状态转换，确保 dirty 与队列行为一致
- [x] 2.2 固化“hash 优先、commit 兜底”上传闭环，并补充关键分支日志
- [x] 2.3 成功同步后统一更新 fid/size/nonce 元数据并清理 pending/dirty

## 3. 恢复与异常处理

- [x] 3.1 挂载恢复时校验 pending 记录有效性，清理孤儿或非法记录
- [x] 3.2 为 hash/commit/finish 任一失败场景保留可重试状态并防止误完成
- [x] 3.3 校准重试策略参数，避免无效任务长期占用上传队列

## 4. 验证矩阵与文档

- [ ] 4.1 手动验证删除场景：单文件删除、目录删除、删除后重启
- [ ] 4.2 手动验证修改场景：覆盖写、追加写、跨目录重命名后修改
- [ ] 4.3 异常验证：网络抖动/中断下重试与恢复行为
- [x] 4.4 更新 skills/qrypt/progress.md，记录本变更的验证结果与已知限制

阻塞说明：当前环境在挂载点写入时返回 `operation not permitted`，4.1-4.3 需在具备写权限的 macOS/FUSE 环境继续执行。
