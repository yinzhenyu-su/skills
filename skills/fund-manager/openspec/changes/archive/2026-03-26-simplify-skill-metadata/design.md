## Context

本次变更是纯粹的简化：删除两个不再产生价值的元数据文件。

## Goals / Non-Goals

**Goals:**
- 删除 `skills/VERSION` 和 `skills/CHANGELOG.md`
- CI 保持不变

**Non-Goals:**
- 不涉及任何功能代码变更
- 不涉及 CI 逻辑变更

## Decisions

本次变更不涉及技术决策，仅为文件删除。

## Risks / Trade-offs

（无显著风险）
