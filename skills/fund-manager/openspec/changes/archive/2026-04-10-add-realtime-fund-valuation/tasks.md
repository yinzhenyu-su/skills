## 1. 基金元数据与行情输入

- [x] 1.1 在 `skills/fund-manager/src/provider/mod.rs` 扩展 `FundData`，增加 `benchmark_name` 和 `benchmark_id` 字段
- [x] 1.2 在 `skills/fund-manager/src/provider/morningstar.rs` 解析晨星 performance 响应中的 `benchmarkName` 和 `benchmarkId`
- [x] 1.3 在 `skills/fund-manager/src/provider/morningstar_market.rs` 增加按指数名称查询实时涨跌幅与市场状态的复用入口

## 2. 实时估值计算与映射规则

- [x] 2.1 新增基金实时估值模块，定义估算结果结构、确认净值输入与估算公式
- [x] 2.2 实现指数映射优先级：手工覆盖规则 > 基准名称规则 > 基金名称规则 > 不估算
- [x] 2.3 为货币、债券、保守混合、行业基金、QDII 和黄金联接实现差异化降级与提示文案

## 3. 命令展示接入

- [x] 3.1 在持仓展示命令中接入实时估值计算，并同时展示确认净值与实时估算值
- [x] 3.2 为休市、无法映射、代理指数和无历史净值场景补充清晰的状态展示
- [x] 3.3 确保估算市值、确认市值、份额和金额沿用现有格式化规则输出
- [x] 3.4 在持仓展示命令中新增基于实时估值的“当日盈亏”和“当日涨跌”展示

## 4. 验证与回归

- [x] 4.1 为指数映射与降级逻辑补充单元测试，覆盖宽基、QDII 代理、黄金和不可估算场景
- [x] 4.2 验证现有净值同步、交易预览和收益计算流程未受到实时估值逻辑影响
- [x] 4.3 运行相关测试并手动检查典型基金在开市与休市状态下的展示结果
- [x] 4.4 手动验证当日实时盈亏与涨跌幅在可估算和不可估算场景下的展示结果
