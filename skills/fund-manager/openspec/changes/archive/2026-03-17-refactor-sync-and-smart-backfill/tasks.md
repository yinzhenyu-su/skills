## 1. 数据访问层优化

- [x] 1.1 在 `src/db.rs` 中新增 `get_earliest_pending_date(code: Option<&str>)` 查询，用于推导智能回溯的起点。

## 2. 同步算法重构

- [x] 2.1 修改 `src/sync.rs` 中的 `calculate_sync_range` 函数，支持根据传入的 `Option<String>` 智能选择：用户指定日期 > 待结算日期 > 默认兜底日期。
- [x] 2.2 调整同步兜底逻辑：全量同步（`--all`）默认回溯 7 天（若无 `pending`），专项同步默认使用当日。

## 3. 命令层交互与分发

- [x] 3.1 修改 `src/main.rs`：当输入 `fund fund sync` 既没有基金标识符也没有 `--all` 标志时，输出错误提示并退出。
- [x] 3.2 优化 `Sync` 分支代码，减少重复的同步代码调用。
- [x] 3.3 在循环同步过程中打印当前处理的任务索引及进度（例如 `[2/5] Syncing 000300...`）。

## 4. 验证与回归

- [x] 4.1 运行所有集成测试，确保同步命令的变动不破坏核心流程。
- [x] 4.2 验证智能回溯功能：构造一笔一个月前的 `pending` 记录，验证运行 `fund fund sync --all` 能否自动覆盖该日期范围。
