## 1. 数据库层增强

- [x] 1.1 在 `src/db.rs` 中定义 `FundWithValuation` 结构体，包含基础信息、净值、日期、份额。
- [x] 1.2 在 `src/db.rs` 中实现 `get_funds_with_valuations` 函数，执行联表查询。
- [x] 1.3 编写单元测试验证 SQL 联查在有/无持仓、有/无净值时的正确性。

## 2. CLI 渲染逻辑

- [x] 2.1 修改 `src/main.rs` 中的 `FundCommands::List` 处理分支。
- [x] 2.2 实现检测当前活跃钱包的逻辑。
- [x] 2.3 使用 `comfy-table` 根据是否有活跃钱包动态构建表格。
- [x] 2.4 实现净值日期格式化（如 `(03-17)`）和金额千分位格式化逻辑。

## 3. 验证与优化

- [x] 3.1 运行集成测试 `cargo test` 确保无回归错误。
- [x] 3.2 在开发环境下运行 `fund fund list` 验证 UI 效果。
- [x] 3.3 测试窄屏下的表格表现，必要时调整列宽或截断。
