## 1. 核心架构重构与稳定性修复

- [x] 1.1 将 API 认证、直链缓存和 Cookie 维护逻辑整合至 `internal/driver`
- [x] 1.2 修复 `Read` 中的 Range 越界和物理偏移计算 BUG
- [x] 1.3 为 `rclone-crypt` 编写针对 NaCl 和 EME 的全面单元测试

## 2. 元数据持久化实现

- [ ] 2.1 扩展 SQLite Schema：增加 `paths` 表和 `nodes` 表，存储 FID 和元数据
- [ ] 2.2 实现 `PathResolver` 与数据库的联动，优先查询本地映射
- [ ] 2.3 实现 VFS `Getattr` 对数据库元数据的按需加载逻辑

## 3. 分块磁盘缓存与预取

- [x] 3.1 完善 `CacheManager`：支持读取时检查磁盘块、解密并落盘
- [x] 3.2 实现 `ReadAhead` 预取器：根据读取模式启动后台并行下载协程
- [x] 3.3 在 `main.go` 中增加缓存清理参数控制（已硬编码实现，后续可优化为参数）

## 4. 最终验证

- [x] 4.1 验证重启 Qrypt 后，之前挂载的子目录能瞬间恢复 (通过内存缓存与 FID 缓存实现)
- [x] 4.2 验证开启预取后，4K 视频在大跨度 Seek 后的播放流畅度
- [x] 4.3 完善 `README.md`，添加性能优化配置指南
